apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-console.fullname" . }}
  labels:
    {{- include "warehouse-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warehouse-console.selectorLabels" . | nindent 4 }}
